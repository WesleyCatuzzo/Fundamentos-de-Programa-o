#include <stdio.h>

int main(void)
{
    int vetorA[20], vetorB[20];
    int i;

    for(i=0; i<20; i++);
    {
        printf("Informe o valor %d", i+1);
        scanf("%d", &vetorA[i]);

    }

    vetorB[i]=vetorA[i];
    if
    {
        vetorB[i]
    }








    return 0;
}
